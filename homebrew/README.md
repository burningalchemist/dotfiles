# Homebrew

Initialize Homebrew and install dependencies using the following command:

```sh
brew bundle install
```

Dump the current list of installed packages to the Brewfile using:

```sh
brew bundle dump --file Brewfile
```
